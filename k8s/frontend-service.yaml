apiVersion: v1
kind: Service
metadata:
  name: zynkly-frontend-service
spec:
  selector:
    app: zynkly-frontend
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080